language: node_js
cache:
  directories:
    - ~/.npm
    - ~/.cache
  override:
    - npm ci
    - $(npm bin)/cypress verify
notifications:
  email: true
node_js:
  - '10'
script:
  - npm run lint
  - npm run transpile
  - npm test -- --record
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
