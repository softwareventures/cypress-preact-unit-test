language: node_js
cache:
  directories:
    - ~/.npm
    - ~/.cache
  override:
    - npm ci
    - $(npm bin)/cypress verify
node_js:
  - '10'
script:
  - npm run lint
  - npm run transpile
  - npm test -- --record
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
